/// <reference types="node" />
import EventEmitter from 'events';
export declare type SSHConfig = {
    remoteHost: string;
    remotePort?: number;
    dbHost?: string;
    dbPort?: number;
    user?: string;
    passphrase?: string;
    identityFile?: string;
};
export declare type Settings = {
    name: string | null;
    adapter: 'mysql' | 'postgresql' | 'sqlite3' | null;
    host: string | null;
    port: number | null;
    user: string | null;
    database: string | null;
    password: string | null;
    filename: string | null;
    projectPaths: string[];
    ssh: SSHConfig | null;
};
declare type PersonalConfig = {
    connections: Settings[];
};
export default class SettingStore extends EventEmitter {
    private personalConfig;
    private state;
    private static instance;
    private constructor();
    static getInstance(): SettingStore;
    getSetting(): Settings;
    getPersonalConfig(): PersonalConfig;
    changeConnection(connectionName: string): Promise<void>;
    setSettingFromFile(personalConfigPath: string, projectConfigPath: string, projectPath: string): Promise<Settings | null>;
    setSetting(setting: Partial<Settings>): void;
}
export {};
